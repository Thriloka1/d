# DP

This repository was created from the local workspace at `C:\Users\hp\Desktop\DP`.

Contents:

- Python scripts for DB loading and file conversion.
- `data/` with example SQL and dataset files.

How this push was attempted:

- Repo name assumed: `DP` (remote: `git@github.com:thriloka1/DP.git`).
- If push fails, create an empty repository named `DP` on GitHub or configure your authentication (SSH key or Personal Access Token) and re-run the push.
